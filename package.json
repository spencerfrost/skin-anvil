{
  "name": "skin-anvil",
  "version": "1.0.0",
  "description": "Skin Anvil — a web application for merging and editing Minecraft skins",
  "main": "server/src/server.js",
  "scripts": {
    "dev": "concurrently \"npm run start --prefix client\" \"npm run dev --prefix server\"",
    "start": "npm run start --prefix server",
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "ci-all": "npm ci && npm ci --prefix client && npm ci --prefix server",
    "build": "npm run build-all",
    "build-all": "npm run build --prefix client && npm run build --prefix server",
    "deploy": "./deploy.sh",
    
    "format:check": "npx prettier --check \"{client,server}/src/**/*.{js,jsx,json}\"",
    "format:fix": "npx prettier --write \"{client,server}/src/**/*.{js,jsx,json}\"",
    "lint:client": "npm run lint --prefix client || true",
    "lint:server": "npx eslint server/src/ --config server/.eslintrc.json || true",
    "lint": "npm run lint:client && npm run lint:server"
  },
  "keywords": [
    "minecraft",
    "skin",
    "merger"
  ],
  "author": "Spencer Frost",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.7"
  }
}
